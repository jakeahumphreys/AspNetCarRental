@model EIRLSSAssignment1.Models.FuelType

@{
    ViewBag.Title = "Details (" + Model.Value + ")";

}

<div>
    <button class="btn btn-primary fa fa-arrow-circle-left" data-toggle="tooltip" title="Back" onclick="location.href='@Url.Action("Index","Admin", new { })';return false;"></button>
    <button class="btn btn-warning fa fa-pen" data-toggle="tooltip" title="Edit" onclick="location.href='@Url.Action("Edit","FuelType", new {id = Model.Id})';return false;"></button>
    <hr />
</div>

<h2>Details (@Model.Value)</h2>

<div>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Value)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Value)
        </dd>
        <dt>
            @Html.DisplayNameFor(model => model.IsInactive)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.IsInactive)
        </dd>

    </dl>
</div>